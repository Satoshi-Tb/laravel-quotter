insert into follows (
   following_user_id,
   followed_user_id,
   created_at,
   updated_at
) values ( 1,
           2,
           now(),
           now() );
insert into follows (
   following_user_id,
   followed_user_id,
   created_at,
   updated_at
) values ( 1,
           3,
           now(),
           now() );
insert into follows (
   following_user_id,
   followed_user_id,
   created_at,
   updated_at
) values ( 1,
           4,
           now(),
           now() );
insert into follows (
   following_user_id,
   followed_user_id,
   created_at,
   updated_at
) values ( 2,
           1,
           now(),
           now() );
insert into follows (
   following_user_id,
   followed_user_id,
   created_at,
   updated_at
) values ( 2,
           4,
           now(),
           now() );
insert into follows (
   following_user_id,
   followed_user_id,
   created_at,
   updated_at
) values ( 3,
           4,
           now(),
           now() );